# MongoDB Connection String
# For local development: mongodb://localhost:27017/moveocrypto
# For production (Render/Railway): Use MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/moveocrypto

# JWT Secret Key - CHANGE THIS IN PRODUCTION!
# Generate a strong random string (at least 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Port
# Render uses PORT environment variable automatically
PORT=5000

# CryptoPanic API Key (Optional)
# Get your free API key from https://cryptopanic.com/developers/api/
CRYPTOPANIC_API_KEY=your-cryptopanic-api-key-optional

# OpenRouter API Key (Optional)
# Get your API key from https://openrouter.ai/
OPENROUTER_API_KEY=your-openrouter-api-key-optional

# Frontend URL (for CORS and OpenRouter)
# Update this with your deployed frontend URL
APP_URL=http://localhost:3000
